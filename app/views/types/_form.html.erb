<div class="row">
  <div class="col-lg-12">
    <div class="card p-5">
      <%= form_with(model: type) do |form| %>
        <% if type.errors.any? %>
          <div style="color: red">
            <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

            <ul>
              <% type.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :nombre, style: "display: block" %>
          <%= form.text_field :nombre, class: 'form-control'%>
        </div>

        <div class="form-group">
          <%= form.label :descripcion, style: "display: block" %>
          <%= form.text_area :descripcion, class: 'form-control', placeholder: 'Agregue una descripción del nuevo tipo de producto que desea'%>
        </div>

        <div class="form-group">
          <%= form.submit "Enviar", class: 'btn btn-primary'%>
        </div>
      <% end %>
    </div>
  </div>
</div>
